import {useParams} from "react-router-dom";
import {Card, CardBody} from "@nextui-org/react";
import {RaceResult} from "../features/race";

export const RaceDetails = () => {

    return (
        <RaceResult/>

    );
};
